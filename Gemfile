source "https://rubygems.org"

gem "jekyll", "~> 3.7", ">= 3.7.2"

group :jekyll_plugins do
  # The paginate is used by the 10deploys-theme. Just having it in the gemspec is not enough.
  gem "jekyll-paginate-v2", "~> 1.9", ">= 1.9.4"
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
gem "10deploys-theme", ">= 0.1.0", :git => "https://github.com/10deploys/theme.git"
